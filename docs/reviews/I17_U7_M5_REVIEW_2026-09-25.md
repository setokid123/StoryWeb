# I17 — Review U7/M5 sau bàn giao (2026-09-25)

## Kết quả đã xác nhận

- M5 commit `a688614` đã ghép dock U7 vào `ReaderPanel`, thêm state cuộn, `IntersectionObserver` cho nav cuối, `inert` khi ẩn và trả focus về đúng nút mở danh sách chương. Không sửa server guard, unlock, DB hay migration.
- Trên nhánh M5: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run build`, `git diff --check` đạt. `test-reader-scroll` 12/12 và `test-reader-preferences` 8/8 đạt khi chạy với `node --experimental-strip-types --no-warnings`. Bàn giao Claude ghi walkthrough Chrome CDP 19/19 trên DB test riêng, kèm ảnh 320/360/768/1280px ở theme Tối.
- U7 có commit mới `1808728` tăng opacity dock và thêm khoảng cuối reader. Commit này **chưa nằm trong M5**; ảnh M5 chụp trước thay đổi đó. Hai nhánh đều sạch.

## Cần hoàn tất trước khi merge

1. Ảnh `m5-w768-dock.png` trên nhánh M5 cho thấy các nhãn trong dock một hàng bị xuống hai dòng, khiến nút hẹp và khó đọc. Antigravity cần giữ nhãn trên một dòng ở tablet/desktop (chẳng hạn `white-space: nowrap` từ 601px và tăng chiều rộng dock phù hợp), vẫn giữ bố cục 2×2 ở 320/360px.
2. Ảnh `m5-w320-dock.png` và `m5-desktop-1280-scrolled-up.png` trên nhánh M5 cho thấy chữ chương lọt qua nền dock Tối. U7 `1808728` đã tăng opacity, nhưng cần chụp lại **bản ghép cuối** để xác nhận nhãn đủ tương phản và nội dung bên dưới đọc được khi dock hiện. Kiểm tra cả theme Sáng, 320/360/768/1280px, reduced motion, fallback không blur và cuối trang.
3. U7 workboard vẫn chưa có ảnh/walkthrough trên commit mới và dòng `Context7`/`Memory` hoặc lý do MCP không chạy. Bổ sung trước khi Codex tích hợp theo `AGENTS.md` và assignment U7. M5 đã có hai dòng này.

Sau khi U7 hoàn tất, Codex sẽ ghép `1808728` hoặc commit kế tiếp với CSS fix `7b5e5be` của M5, chạy lại build/test và kiểm tra trình duyệt. Chưa merge `main`, chưa push/deploy.

Context7: không cần (review code/CSS, không đổi API thư viện). Memory: đã tra U7/M5/I16; đã cập nhật I17.
